function printingArr(input, delimiter){
let result = input.join(delimiter)

console.log(result);
}

printingArr(['How about no?', 
    'I',
    'will', 
    'not', 
    'do', 
    'it!'], 
    '_'
    )
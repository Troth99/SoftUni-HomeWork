Деструктурирането на масив е техника в програмирането, която позволява да извлечем стойности от масив и да ги присвоим директно на отделни променливи.
 Това прави кода по-четим и удобен, особено когато искаме да извлечем само определени стойности от масив. Деструктурирането се използва в езици като JavaScript, 
Python и други, които поддържат тази функционалност.

Пример в JavaScript:
Ако имаме масив с няколко стойности и искаме да ги запазим в отделни променливи:

javascript
Copy code
const arr = [1, 2, 3];
const [a, b, c] = arr;

console.log(a); // 1
console.log(b); // 2
console.log(c); // 3
В този пример [a, b, c] = arr е деструктуриране, което автоматично присвоява първата стойност на a, втората на b, и третата на c.